import { Router } from "express";

const router = Router();
/*
router.get("/vanzare/:id", homeController.getPropertySale);

router.get("/inchiriere/:id", homeController.getPropertyRent);
*/
export default router;
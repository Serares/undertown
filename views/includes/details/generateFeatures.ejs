<!-- TODO component that handles rendering of features for all property types
    features should be rendered in a loop, not hardcoded
    hold the display values and actual values in a data structure
-->